title: Sigrun
description: > 
  Din beskrivelse her

# Site Author
author:
  name: "Sigrun"
  avatar: "profile.png"  # Du kan legge til et profilbilde senere
  bio: "Din bio"
  location: "Norge"

# Site Settings
locale: "nb-NO"  # Setter norsk som språk
remote_theme: "academicpages/academicpages.github.io"

# Defaults for pages
defaults:
  # _pages
  - scope:
      path: ""
      type: pages
    values:
      layout: single
      author_profile: true

# Site navigation
include: ["_pages"]
